import SwiftUI

// MARK: - (WhenNextView)
struct WhenNextView: View {
// (goal) (user can see when the next quiz is)
    // MARK: - (body)
    var body: some View {
        Text("WhenNextView")
    }
}
