import SwiftUI

// MARK: - (QuizView)
struct QuizView: View {
// (goal) (user can get quizzed on her texts) (to know them)
    // MARK: - (body)
    var body: some View {
        Text("QuizView")
    }
}
